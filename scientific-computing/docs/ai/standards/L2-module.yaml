# L2: 模块级规范

## 模块结构

每个模块包含：
- __init__.py: 公开 API 导出 + 模块文档字符串
- 功能文件: 按功能拆分为独立 .py 文件

## 依赖规则

- 模块间禁止循环依赖
- 所有模块依赖 numpy
- 仅 stats 和 interp 依赖 scipy

## 测试

- 每个模块对应一个测试文件 tests/test_{module}.py
- 测试函数覆盖率 ≥ 90%
- 性能基准：核心运算不超过 SciPy 同功能的 5 倍 wall-clock 时间
